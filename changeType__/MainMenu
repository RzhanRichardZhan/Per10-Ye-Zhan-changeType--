class MainMenu {
  class se
  String[] filenames;
  final int RECTS = 200, TEXTX, TEXTY;
  void setup()
    java.io.file folder = new java.io.File(dataPath("/levels"));
    java.io.FilenameFilter txtFilter = new java.io.FilenameFilter() {
     boolean accept(File dir, String name) {
      return name.toLowerCase().endsWith(".txt");
    }
    filenames = folder.list(txtfilter);
    
  };

void mousePressed(){
  if(selected){
    //loadlevel();
   }
}

void draw(){
  //background();
  for(int k = 0; k < filenames.length(); k++){
    rect(
    text(
  }
}
}
